python -m http.server 8080
uvicorn main:app --reload

logujemy sie tylko mailem!!!!!!


PIATEK:
dokumentacje napisac do users oraz dodac


NIEDZIELA:
Dokonczyc notes endpointy + dokumentacja notes


OSTATECZNE RZECZY DO WYKONANIA: + rozrysowac jak wyglada testowo dla przema baza danych
DODAC DO FILTORWANIA DANYCH:
# Model kategorie
class Categories(Base):
    __tablename__ = 'categories'
    
    id = Column(Integer, primary_key=True, index=True)
    name = Column(String, nullable=False)
    note_id = Column(Integer, ForeignKey('notes.id'), nullable=False)

    # Relacja z notatkami
    note = relationship("Note", back_populates="categories")
do tego dodac endpointy z tym zwiazane